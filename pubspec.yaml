name: redux_playground
description: A playground to test flutter_redux.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  flutter_redux: ^0.6.0
  redux: ^4.0.0
  collection: any

dev_dependencies:
  flutter_test:
    sdk: flutter

  test: any

flutter:
  uses-material-design: true
